- **`data.streaming` is honoured for a single HuggingFace Hub dataset name (#710 by @jagadeepmamidi, closes #689).** `_load_one_hub_dataset` now forwards `streaming=True` (and `buffer_size` shuffle) to `datasets.load_dataset`, then materialises up to `MAX_REMOTE_ROWS`. An all-hub interleave list with streaming stays schema-rejected.
