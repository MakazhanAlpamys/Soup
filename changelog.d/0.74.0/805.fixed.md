- **Training settings now reach PRM, MoLE routing, PPO and unlearning (#805).** These trainers now honor the configured optimizer, scheduler, warmup, weight decay, gradient clipping and batch-size behavior instead of silently using backend defaults. Unlearning also applies the configured sequence length, quantization and LoRA options; `batch_size: auto` now reports a clear configuration error for that loop.
