- `data.interleave`'s `over`/`probs` strategies could place the same row (a source's original plus its `over`/`probs`-padded copy) in both `train` and `val`; `val_split` now runs before the padding on both the local-file and HF-hub interleave paths (#680 by @AmirF194 in #701).
