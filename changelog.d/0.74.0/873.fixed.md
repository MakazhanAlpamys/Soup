- `soup doctor` reports the training stack as the `[train]` extra instead of seven required packages: a core-only install now shows a single `pip install "soup-cli[train]"` hint rather than seven MISSING failures, every suggestion is built from the extra name (keeping pyproject's declared ceilings) and double-quoted so it survives cmd.exe, and a missing core dependency exits non-zero so `doctor` can gate CI. `soup version --full` derives installed extras from metadata, so `train`, `mcp` and any future extra are reported without a hand-kept list (#828 by @blackcoderx in #873).
