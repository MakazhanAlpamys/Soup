- **SFT `packing: true` no longer raises `TypeError` on TRL 0.29 (#709 by @jagadeepmamidi, closes #691).** `packing` is an `SFTConfig` field; passing it as an `SFTTrainer` kwarg is rejected because `__init__` has no `**kwargs`. Soup now carries it through `_as_sft_config`. `packing_cross_doc_attn_mask` is schema-rejected (`packing_strategy` allowlist is `bfd` / `bfd-requeue` / `wrapped`).
