- Layer streaming now shards `embed_tokens` and an untied `lm_head` separately (#324) and reuses
  one vocabulary-sized device buffer for both, instead of keeping both matrices resident.
  Tied embeddings keep their existing one-matrix memory requirement.
