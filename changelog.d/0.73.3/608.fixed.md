- **`soup serve --backend mii` now applies the model's chat template and reports a real `finish_reason` (#606 in #608).** The MII `/v1/chat/completions` handler hand-rolled a `System:/User:/Assistant:` prompt instead of calling `build_chat_prompt`, so a chat-tuned model saw a format it never trained on — the defect class behind the Llama-3.1-8B run-on loop fixed for vLLM in #332. It also hardcoded `finish_reason: "stop"`, so a client could not tell a truncated answer from a completed one; it now calls `resolve_finish_reason` like the vLLM backend does (#333). `serve` loads the served model's tokenizer for the MII path exactly as it does for vLLM, and warns identically when the model ships no template. Behaviour is unchanged for a model without one: the shared builder's fallback is byte-identical to the removed block. Adds `tests/test_mii_serve.py`, the module's first test file.
