- Re-enabled `/v1/tools/bash` execution with OS-level namespace/sandbox isolation (#151). Note this is a breaking change: `serve()` now raises `typer.Exit(code=2)` when run with `--host 0.0.0.0` without `--tool-auth-token`, whereas previously it only printed a warning.
