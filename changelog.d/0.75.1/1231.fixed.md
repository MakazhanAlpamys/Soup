- **`soup expect` scans all dataset formats for PII, token length, and refusals (#1231 by @kok-o).** The dataset expectation gate previously only inspected a few field names (`text`, `instruction`, `output`, string `messages`), missing ShareGPT, DPO (string and conversational `chosen`/`rejected`), KTO (`completion`), Alpaca `input`/`system`, multimodal content parts, tool calling, and sentence embedding fields. The text extractors now cover all format signatures declared in `FORMAT_SIGNATURES`, verify assistant-side refusal patterns across formats, and fail closed when rows lack extractable text.
