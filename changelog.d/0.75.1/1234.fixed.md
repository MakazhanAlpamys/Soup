- Refuse `training.batch_size: 1` with `embedding_loss: contrastive` at config parse and at setup; floor `batch_size: auto` at 2 for contrastive and set `dataloader_drop_last` to prevent zero loss and zero gradient from one-row batches (#1234).
