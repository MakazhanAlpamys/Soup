- **`training.loraplus_lr_ratio` with `optimizer: apollo_adamw`, `lomo` or `adalomo`, or with `use_galore`, is now refused at config load (#1210 by @mohdUwaish59).** LoRA+ asks transformers for its optimizer without the model, and transformers can only build those three optimizers from one, so such a config passed `soup train`'s config load and failed only after the base model had been downloaded and loaded. LoRA+ with GaLore was likewise refused only at setup. Both now fail at load with a message that names the two fields.
