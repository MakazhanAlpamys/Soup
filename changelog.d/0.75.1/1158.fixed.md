- **Hardened MCP run-log opens against directory symlinks in parent path and pre-planted hardlinks (#1158 by @MakazhanAlpamys).**
  Following #1138, `open_no_follow` now inspects parent directory components for symlinks and reparse points when `check_parent=True`, and rejects hardlinked regular files (`st_nlink > 1`). `ExecutionManager.execute()` passes `check_parent=True`, ensuring child stdout redirection cannot escape `.soup/mcp-runs/` via a directory symlink or overwrite a victim file via a pre-planted hardlink.
