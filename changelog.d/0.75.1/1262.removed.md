- Removed the dormant `auto_quant` compatibility module (`soup_cli/utils/auto_quant.py`) that `soup serve --auto-quant` stopped using when it began refusing with exit 2 in #816; the refusal itself is unchanged and still covered by regression tests (#1262).
