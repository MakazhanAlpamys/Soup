- Subclass `TrainerCallback` lazily for `BetaScheduleCallback` and `RefModelRegenCallback` to prevent `AttributeError` crashes at step 0 during DPO/IPO training; refuse `training.dpo_ref_regen_epochs` with LoRA enabled (`lora.r > 0`) at config load time (#1229).
