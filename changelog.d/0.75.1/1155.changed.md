- **Wired FP8 and int8 QAT for audio SFT; refused unsloth and streaming with advanced precision (#1124 by @kok-o in #1155).** `SFTTrainerWrapper._setup_audio_transformers` now calls `_apply_quantization_aware` post-LoRA, bringing SFT audio to parity with text SFT for FP8, fp8_attention, and int8 QAT. Configurations across all tasks specifying `stream_layers: true` now reject `quantization_aware` (both FP8 and int8 QAT), `fp8_attention`, and `nvfp4` at schema validation. `backend: unsloth` rejects `quantization_aware: fp8`, `fp8_attention`, and `nvfp4` at schema validation with an actionable message directing to switch to `backend: transformers`, and registers `REJECTED` in `backend_support.py`.
