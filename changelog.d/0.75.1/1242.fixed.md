- Refuse rows with no causal-loss target at setup in `task: distill` with human-facing row number and `data.max_length`, matching SFT behavior, and return finite 0.0 CE loss in `compute_loss` if a microbatch contains zero targets (#1242).
