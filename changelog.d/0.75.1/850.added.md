- **Added ready-made `deepseek-v4-pro-dpo` and `deepseek-v4-pro-grpo` recipes for deepseek-ai/DeepSeek-V4-Pro (#275 / #850).** Completes the task trio with the existing SFT sibling over the 1.6T-class flagship MoE architecture (LoRA r32/a64, 4-bit quantization, `moe_lora: true`, `moe_aux_loss_coeff: 0.01`, `gradient_checkpointing: true`). DPO uses `max_length: 4096`, `epochs: 1`, `lr: 5e-6`, and `gradient_accumulation_steps: 32`. GRPO uses `max_length: 8192`, `epochs: 3`, `lr: 1e-5`, and `gradient_accumulation_steps: 16`. Catalog count 174 -> 176.
