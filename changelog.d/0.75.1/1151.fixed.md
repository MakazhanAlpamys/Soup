- **`training.moe_lora` now works on `classifier`, `reranker`, `cross_encoder`, `distill` and `unlearn`, and is refused on `asr` and `moe_lora_routing` (#1151 by @Srinivasan8888 in #PENDING).**
  The five tasks accepted `moe_lora: true` and never read it; each now picks the
  expert targets through the same helper as every other MoE-wired trainer.
  `asr` trains only Whisper, which has no experts, and `moe_lora_routing` builds
  no LoRA adapter, so on those two the flag is refused at config load with a
  message naming the task. `prm` still loads it unread, pending a ruling.
