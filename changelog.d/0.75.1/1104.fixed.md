- **`soup loop canary` now routes keyed transformers requests at the configured traffic share and automatically clears a regressing canary (#815 by @akkupratap323 in #1104).** Serve records rollout-scoped generation outcomes for stable and canary buckets; `soup loop watch` evaluates those observations and persists a rollback after a `MAJOR` verdict, while explicit adapter selection and the 100% activation endpoint keep their existing behavior.
