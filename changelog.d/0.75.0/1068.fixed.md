- **Rich ReprHighlighter split percentage strings on colour terminals, failing raw CLI output assertions (#1068).** `tests/test_v07133.py::TestDraftMeasureCli::test_below_min_acceptance_exits_two` and sibling assertions checked percentage strings like `"60.0%"` directly against `result.output`. Rich styles numbers with SGR escapes and leaves `%` unstyled, inserting escape sequences between the digits and the percent sign on colour-capable terminals; this passed on Windows where colour is auto-disabled and turned CI red under `FORCE_COLOR=1`. These assertions now route output through `_plain(...)`, and `tests/test_cli_help_assertions_are_ansi_safe.py` adds a dedicated second AST scanner (`find_raw_cli_output_assertions`) that tracks variable derivation and guards the entire test suite against raw or unnormalised percentage CLI output assertions, backed by `TestTheCliOutputScannerCanActuallyFail` coverage.
