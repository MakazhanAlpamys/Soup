- **`training.nvfp4` and `training.fp8_attention` are now applied on `task: sft` post-LoRA (#800 by @kok-o in #1022).** `SFTTrainerWrapper._apply_quantization_aware` now delegates to `apply_v028_speed_memory` with `skip_cut_ce=True` so NVFP4 and FP8 attention projections apply on SFT runs without double-patching Cut-CE. Both flags are also gated at schema time against non-v028 tasks and the MLX backend to prevent silent no-ops.
