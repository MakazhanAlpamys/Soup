- **Fixed `glm-4.6-sft`, `glm-5-sft`, and `minimax-m2-sft` declared size labels and added a ratchet checking recipe sizes against recorded base parameter counts (#1161 by @MakazhanAlpamys in #1168).**
  Sparse MoE models across the catalog are sized by total parameters (e.g. `MiniMax M3` 428B, `DeepSeek V3` 671B, `GLM-5.1` 754B). The three recipes previously carried placeholder `size="9B"` despite having bases with 357B, 754B, and 230B parameters respectively. Added recorded base parameter counts to `tests/fixtures/recipe_base_architectures.json` and introduced `tests/test_recipe_sizes.py` verifying that all resolvable recipes match their base model size within a 30% tolerance.
