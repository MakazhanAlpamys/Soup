- `training.use_flash_attn` and `training.use_liger` are now rejected at config load for any task outside `sft`/`tts`, the only trainers that read either flag. `soup autopilot` stops auto-enabling both for DPO/GRPO/pretrain on Ampere or newer, where they previously built a config the CLI's own validation would then refuse for a package that task never used (#806 by @MakazhanAlpamys in #983).
