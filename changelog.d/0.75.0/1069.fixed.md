- **Reject and report `loss_watchdog`, `loss_spike_recovery`, and `grad_accum_auto_tune` on `backend: mlx` (#1069).** On `backend: mlx`, no `SoupTrainerCallback` is attached and `mlx-lm` provides no graceful early-stop mechanism or unified memory VRAM pressure signals. All three callback-monitoring settings are now rejected at schema validation time naming `backend='mlx'` (extending `_validate_callback_monitoring_task_compat`), declared as unread in `backend_support.py`'s `_MLX_SFT` registry table for `soup doctor --config`, and warned about at runtime in `trainer/mlx_sft.py::_check_unsupported` (#1069 by @kok-o).
