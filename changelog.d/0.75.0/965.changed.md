- **`soup doctor --config` and the MLX trainer now report four more CUDA-only settings that MLX ignores (#958 by @dchaudhari7177 in #965).** `training.use_mod`, `training.moe_lora`, `training.quantization_aware` and `training.use_fsdp2_compile` load on `backend: mlx` but nothing on that path reads them; a config setting any of them now gets a doctor row and an `MLX backend ignores:` warning, and the all-clear reads `None of the 13 setting(s)`. Five other CUDA-only fields (`use_cut_ce`, `kernel_auto_compose`, `use_longlora`, `activation_offloading`, `packing_cross_doc_attn_mask`) are already refused at config load on MLX and are not listed.
