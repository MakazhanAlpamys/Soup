- `soup doctor` no longer reports the whole training stack as "not installed" when only some of it is missing: with six of seven `[train]` packages present it now says `Training stack incomplete, missing: <names>` and puts the group install line on its own row, instead of the misleading `Training stack not installed` and a `pip install "soup-cli[train]"` that is a no-op for the six already satisfied. A fully-absent stack keeps its previous wording, the exit code stays 0, and the CUDA wheel-index step is only suggested when torch itself is missing (#875 by @blackcoderx in #888).
